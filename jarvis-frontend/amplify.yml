version: 1
frontend:
  phases:
    preBuild:
      commands:
        - echo "No preBuild commands needed for static HTML"
    build:
      commands:
        - echo "No build commands needed for static HTML"
  artifacts:
    baseDirectory: .
    files:
      - '**/*'
